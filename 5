# Switch to the target branch (where you want to merge INTO)
git checkout main

# Merge another branch into the current branch
git merge <branch-name>

# Example workflow
git checkout main
git merge feature/user-authentication
