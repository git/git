#!/bin/sh
#
# An example hook script to verify what is about to be cummitted
# by applypatch from an e-mail message.
#
# The hook should exit with non-zero status after issuing an
# appropriate message if it wants to stop the cummit.
#
# To enable this hook, rename this file to "pre-applypatch".

. git-sh-setup
precummit="$(git rev-parse --git-path hooks/pre-cummit)"
test -x "$precummit" && exec "$precummit" ${1+"$@"}
:
