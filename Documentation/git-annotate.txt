but-annotate(1)
===============

NAME
----
but-annotate - Annotate file lines with cummit information

SYNOPSIS
--------
[verse]
'but annotate' [<options>] <file> [<revision>]

DESCRIPTION
-----------
Annotates each line in the given file with information from the cummit
which introduced the line. Optionally annotates from a given revision.

The only difference between this command and linkbut:but-blame[1] is that
they use slightly different output formats, and this command exists only
for backward compatibility to support existing scripts, and provide a more
familiar command name for people coming from other SCM systems.

OPTIONS
-------
include::blame-options.txt[]

SEE ALSO
--------
linkbut:but-blame[1]

GIT
---
Part of the linkbut:but[1] suite
