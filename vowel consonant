#include <stdio.h>
#include <stdlib.h>

int main()
{
    char ch;
    printf("Enter Character Value\n");
    scanf("%c",&ch);

    if(ch == 'a' || ch == 'e' || ch == 'i' ||ch == 'o' || ch == 'u')
    {
        printf("Vowel");
    }
    else
    {
        printf("Consonant");
    }
    return 0;
}
